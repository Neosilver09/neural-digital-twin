# Notebooks

Exploratory Jupyter notebooks live here: prototyping filters, visualizing
state estimates, comparing model variants. Anything that becomes a stable,
reusable component should graduate into `src/neural_digital_twin/`.
